import React from 'react';
import ScreenWrapper from '../../components/ScreenWrapper';

const DataScreen = () => {
    return (
        <ScreenWrapper
            header={<></>}
            content={<></>}
            dialogs={<></>}
        />
    );
};

export default DataScreen;
